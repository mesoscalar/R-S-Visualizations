{
  "name": "r-s-visualizations",
  "private": true,
  "version": "0.1.0",
  "description": "Monorepo: shared math kernel (@rsvis/math) + gallery (R&S Vol. 2 visual companion) + bundle (forthcoming instrument).",
  "workspaces": [
    "math",
    "gallery",
    "bundle"
  ],
  "scripts": {
    "test": "npm run test --workspaces --if-present",
    "build:math": "npm run build --workspace=@rsvis/math",
    "build:gallery": "npm run build --workspace=@rsvis/gallery",
    "build:bundle": "npm run build --workspace=@rsvis/bundle",
    "build": "npm run build:gallery && npm run build:bundle"
  }
}
