{
  "name": "@rsvis/math",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Shared framework-agnostic math kernel (integrators, quaternions, quadrature, winding) for the R-S-Visualizations monorepo.",
  "exports": {
    ".": "./src/index.ts"
  },
  "types": "./src/index.ts",
  "scripts": {
    "test": "vitest run",
    "build": "tsc --noEmit",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.5.4",
    "vitest": "^2.0.5"
  }
}
